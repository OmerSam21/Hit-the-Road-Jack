class Sprites {
    /** Draws the player at a specific location */
    function void drawPlayer(int x, int y) {
        do Screen.setColor(true);
        do Screen.drawRectangle(x, y, x + 16, y + 16);
        return;
    }
}